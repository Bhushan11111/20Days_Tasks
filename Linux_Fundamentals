Task 1: Navigations commands

cd pwd find ls tree

Day 5: 
mv source destination
mv also used to rename
mv current name chnaged name
rm cmd used remove any files
rm -r remove directory recursively
cp used for COPY files same as mv
